{
  "name": "ryans_typescript_boilerplate",
  "version": "0.0.1",
  "description": "Ryan's Typescript Boilerplate",
  "main": "build/index.js",
  "scripts": {
    "start": "node -r @swc-node/register src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc-node/register": "^1.10.9",
    "@types/node": "20.12.7",
    "gts": "^5.3.1",
    "typescript": "5.1.3"
  }
}
